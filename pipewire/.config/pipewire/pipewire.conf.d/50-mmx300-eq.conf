context.modules = [
  { name = libpipewire-module-parametric-equalizer
    args = {
      equalizer.filepath = "/home/marco/.config/pipewire/mmx300_eq.txt"
      equalizer.description = "MMX 300"
      audio.channels = 2
      audio.position = [ FL FR ]
      capture.props = {
        node.name = "mmx300-eq"
        priority.driver = 1500
      }
      playback.props = {
        node.name = "mmx300-eq-output"
      }
    }
  }
]
